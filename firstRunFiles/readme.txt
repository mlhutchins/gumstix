To setup up the Gumstix as a WWLLN host:

1. Connect to the internet
	a. Use dhclient to start DHCP if connected via serial
	b. Setup static IP and reboot

2. Run install.sh - Installs necessary packages

3. Run setup.sh - Sets up users and configuration files

4. Reboot and login as sferix
